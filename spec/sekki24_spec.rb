# frozen_string_literal: true

RSpec.describe Sekki24 do
  it "has a version number" do
    expect(Sekki24::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
